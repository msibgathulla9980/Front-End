import { NoteSearchPipe } from './note-search.pipe';

describe('NotesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
